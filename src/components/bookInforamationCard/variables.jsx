export const bookCover = (item) => item.volumeInfo.imageLinks.thumbnail;
export const bookTitle = (item) => item.volumeInfo.title;
export const authors = (item) => item.volumeInfo.authors;
export const categories = (item) => item.volumeInfo.categories;